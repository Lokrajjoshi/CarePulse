# Data dictionary

`customers` identifies synthetic segments and regions. `cases` stores journey-level operational fields. `interactions` stores timestamped messages, actor, ground-truth sentiment, and friction flags. `promises` stores commitments and actual update timing. `interventions` stores recovery actions. `feedback` stores CSAT, DSAT, recommendation score, final sentiment, and repeat-contact outcome. `agents` contains synthetic team and experience bands only.

