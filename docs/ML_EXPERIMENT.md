# Experimental ML Benchmark

The production recommendation remains rule-based. A future benchmark can use scikit-learn Logistic Regression with first response, silent wait, transfers, repeat contact, missed promise, sentiment, resolution, escalation, and effort as features. Train/test splits, accuracy, precision, recall, F1, confusion matrix, and coefficients should be reported without tuning for impressive numbers. This reference keeps the benchmark clearly experimental.

