# Azure architecture

Azure App Service or Container Apps hosts FastAPI; Azure Database for PostgreSQL stores shared data; Application Insights and Azure Monitor provide logs and health alerts. HTTPS, managed identity where possible, and environment-based secrets are expected. Local SQLite remains independent.

