# SQL learning guide

Start with `01_basic_select.sql`, then filter DSAT, group by issue, and join feedback. `06_promise_reliability.sql` demonstrates conditional aggregation. The key learning question is always: what business decision would this result inform? SQLite uses `1` and `0` for booleans; PostgreSQL accepts the same concept with boolean syntax adjustments.

