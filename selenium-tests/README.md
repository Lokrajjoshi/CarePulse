# Selenium suite

Requires Java 17+, Maven, Chrome, and a running CarePulse server. From this folder run `mvn test`. Selenium Manager normally obtains the matching browser driver. The suite is intentionally a small Page Object Model starter; add tests under `src/test/java` as the UI stabilises.

